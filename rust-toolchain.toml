# Pin the toolchain so CI/local format + lint with the exact rustfmt/clippy the
# repo targets (line-breaking heuristics and lints change between releases).
# Kept in lockstep with the CI action + the rust:* build image by Renovate.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
